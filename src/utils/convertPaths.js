export const convertPaths = (page) => {
    console.log(page);

    switch (page) {
        case 'Tienda':
         return "/shop"
        
        case 'Descubrir orígenes':
            return "/shop"
       
      }

}